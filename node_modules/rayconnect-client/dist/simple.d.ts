/// <reference types="node" />
import { EventEmitter } from 'events';
export default class WebSocketClient {
    private event;
    private ws;
    private globalInterval;
    autoReconnectInterval: number;
    constructor(link: any);
    reconnect(): void;
    private send;
    emit(name: any, data: any): void;
    on(event: string | symbol, listner: (...args: any[]) => void): EventEmitter;
    once(event: string | symbol, listner: (...args: any[]) => void): EventEmitter;
    off(event: string | symbol, listner: (...args: any[]) => void): EventEmitter;
    close(): void;
    readData(event: string | symbol, listner: (...args: any[]) => void): EventEmitter;
}
